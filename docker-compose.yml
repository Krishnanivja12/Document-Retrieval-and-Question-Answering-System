version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/app:/app/app
      - ./data:/app/data
    env_file:
      - ./backend/.env
    command: uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
